---@meta

---@class tractor_beam
local tractor_beam = {}

---Tries to pick up a random item in the robots' vicinity (a 9×9 area with the robot at its center).
--- @return boolean # true if an item was picked up, false otherwise
function tractor_beam.suck() end

return tractor_beam
